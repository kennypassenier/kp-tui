[package]
name = "kp-tui-palette"
description = "The kp-themes palettes, generated from the package's own tokens"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
readme = "../../README.md"
keywords = ["theme", "palette", "colors", "tui", "terminal"]
categories = ["command-line-interface"]

[dependencies]
